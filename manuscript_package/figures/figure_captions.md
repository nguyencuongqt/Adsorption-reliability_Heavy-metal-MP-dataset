# Figure Captions

**Figure 1.** Performance drop across the validation hierarchy. The figure compares `random_cv`, `group_exp`, `group_aut`, and the optional `leave_one_study_out` stress test, showing how predictive performance deteriorates as validation moves from row-level interpolation toward study-level transfer.

**Figure 2.** Mechanism-group importance across methods. The heatmap compares the relative importance of solution chemistry, surface area and porosity, metal identity, surface functionality, polymer identity, and aging state across coefficient, permutation, gain, and SHAP-based interpretation methods.

**Figure 3.** Null-feature benchmarking across importance methods. The figure summarizes mean null-feature importance share and highlights which methods are most vulnerable to spurious attribution.
